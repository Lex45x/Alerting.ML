<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonia="http://reactiveui.net"
        xmlns:routing="clr-namespace:Alerting.ML.App.Routing"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Alerting.ML.App.Views.MainWindow"
        Title="Alerting.ML.App">
    <Interaction.Behaviors>
        <DetachedFromVisualTreeTrigger>
            <InvokeCommandAction Command="{Binding DisposeCommand}" />
        </DetachedFromVisualTreeTrigger>
    </Interaction.Behaviors>
    <avalonia:RoutedViewHost Router="{Binding Router}">
        <avalonia:RoutedViewHost.DefaultContent>
            <TextBlock Text="If you see this then something is VERY broken" />
        </avalonia:RoutedViewHost.DefaultContent>
        <avalonia:RoutedViewHost.ViewLocator>
            <routing:AppViewLocator />
        </avalonia:RoutedViewHost.ViewLocator>
    </avalonia:RoutedViewHost>
</Window>